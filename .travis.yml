language: python
python:
  - "2.7"
  - "3.5"
# command to install dependencies
addons:
 apt_packages:
   - swig
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - make test
